node_modules/
.venv/
dist/
dist-ssr/
target/
artifacts/local-vision/
.codex-run-logs/
*.log
*.local
.DS_Store
production/
study/
**/production/
*apikey*.txt
.codex-tmp/
.mimosa/
.zcode/
yolo26n.pt
.mimocode/
.tmp/
__pycache__/
*.pyc

# ── Local scratch and downloaded reference material (G4) ─────────────────────
# These accumulated 141 MB of untracked, unignored files: `git status` was noisy and
# a `git add -A` would have committed them. Everything here is derived or
# re-downloadable, so it is ignored rather than tracked.
.dsh-tmp/
.tmp-*/
.tmp-*
runs/

# Generated support bundles embed local task data (redacted, but still this machine's
# activity). They are produced on demand by `pnpm diagnostics` and are not source.
docs/qa/diagnostics/
