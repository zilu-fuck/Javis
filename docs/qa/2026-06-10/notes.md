# QA Notes

This dated folder is prepared for strict product workflow QA evidence collected
from packaged desktop runs.

Current status:

- `trend-hot-list-live` has a playbook under `trend-hot-list/`, but no real
  packaged-app screenshot or output has been captured yet.
- `code-agent-live-provider`, `git-remote-pr-writes`, and
  `release-and-rollback` remain blocked until their live/package evidence is
  collected and cross-checked.
